class AppConfig {
  // 텍스트 이동 속도 (Marquee 전용)
  static const double moveVelocity = 70.0;
  // Marquee 관련
  static const double marqueeBlankSpace = 100.0;
  static const double marqueeLineHeight = 1.0;

  // 전광판 사이즈
  static const double previewWidth = 1600.0;
  static const double previewHeight = 900.0;

  // 기본 텍스트
  static const String defaultDisplayText = '여기에 텍스트를 입력하세요';

  // 폰트 스타일
  static const String fontFamily = 'Pretendard';
  static const double defaultFontSize = 80.0;

  // 기타 UI 설정
  static const double iconSize = 24.0;
}
